import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { IPluginRegistry, LoopworkPlugin } from '../plugin/index'

/**
 * index Tests
 * 
 * Auto-generated test suite for index
 */

describe('index', () => {

  describe('IPluginRegistry', () => {
    test('should be defined', () => {
      expect(IPluginRegistry).toBeDefined()
    })
  })

  describe('LoopworkPlugin', () => {
    test('should be defined', () => {
      expect(LoopworkPlugin).toBeDefined()
    })
  })
})
