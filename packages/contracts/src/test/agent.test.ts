import { describe, expect, test } from 'bun:test'
import type { AgentResponse, ITool, IAgent, IAgentRegistry } from '../agent'

describe('agent', () => {
  test('should import all types without error', () => {
    expect(true).toBe(true)
  })
})
