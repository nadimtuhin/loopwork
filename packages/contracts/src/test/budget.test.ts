import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { UsageLimit, TokenUsage, UsageEntry, UsageSummary, DailySummary, ICostTracker, IBudgetManager } from '../budget'

/**
 * budget Tests
 * 
 * Auto-generated test suite for budget
 */

describe('budget', () => {

  describe('UsageLimit', () => {
    test('should be defined', () => {
      expect(UsageLimit).toBeDefined()
    })
  })

  describe('TokenUsage', () => {
    test('should be defined', () => {
      expect(TokenUsage).toBeDefined()
    })
  })

  describe('UsageEntry', () => {
    test('should be defined', () => {
      expect(UsageEntry).toBeDefined()
    })
  })

  describe('UsageSummary', () => {
    test('should be defined', () => {
      expect(UsageSummary).toBeDefined()
    })
  })

  describe('DailySummary', () => {
    test('should be defined', () => {
      expect(DailySummary).toBeDefined()
    })
  })

  describe('ICostTracker', () => {
    test('should be defined', () => {
      expect(ICostTracker).toBeDefined()
    })
  })

  describe('IBudgetManager', () => {
    test('should be defined', () => {
      expect(IBudgetManager).toBeDefined()
    })
  })
})
