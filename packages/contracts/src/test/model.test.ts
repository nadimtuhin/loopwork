import { describe, expect, test } from 'bun:test'
import type { IModelRegistry, ModelPresets, ModelRegistryStats, IModelDefinition, ModelPreset } from '../model'

describe('model', () => {
  test('should import all types without error', () => {
    expect(true).toBe(true)
  })
})
