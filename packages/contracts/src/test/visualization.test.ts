import { describe, expect, test } from 'bun:test'
import type { GraphNode, GraphEdge, GraphData, RenderOptions, IGraphRenderer, DependencyContext, IDependencyVisualizer } from '../visualization'

describe('visualization', () => {
  test('should import all types without error', () => {
    expect(true).toBe(true)
  })
})
