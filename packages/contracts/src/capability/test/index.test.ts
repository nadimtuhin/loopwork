import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { ICapabilityRegistry } from '../capability/index'

/**
 * index Tests
 * 
 * Auto-generated test suite for index
 */

describe('index', () => {

  describe('ICapabilityRegistry', () => {
    test('should be defined', () => {
      expect(ICapabilityRegistry).toBeDefined()
    })
  })
})
