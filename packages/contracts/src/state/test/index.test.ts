import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { StateSnapshot, StateManagerConfig, LoadStateResult, IStateManager, IStateManagerConstructor } from '../state/index'

/**
 * index Tests
 * 
 * Auto-generated test suite for index
 */

describe('index', () => {

  describe('StateSnapshot', () => {
    test('should be defined', () => {
      expect(StateSnapshot).toBeDefined()
    })
  })

  describe('StateManagerConfig', () => {
    test('should be defined', () => {
      expect(StateManagerConfig).toBeDefined()
    })
  })

  describe('LoadStateResult', () => {
    test('should be defined', () => {
      expect(LoadStateResult).toBeDefined()
    })
  })

  describe('IStateManager', () => {
    test('should be defined', () => {
      expect(IStateManager).toBeDefined()
    })
  })

  describe('IStateManagerConstructor', () => {
    test('should be defined', () => {
      expect(IStateManagerConstructor).toBeDefined()
    })
  })
})
