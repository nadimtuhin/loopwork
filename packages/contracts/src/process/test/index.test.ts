import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { ISpawnedProcess, ISpawner, IProcessManager } from '../process/index'

/**
 * index Tests
 * 
 * Auto-generated test suite for index
 */

describe('index', () => {

  describe('ISpawnedProcess', () => {
    test('should be defined', () => {
      expect(ISpawnedProcess).toBeDefined()
    })
  })

  describe('ISpawner', () => {
    test('should be defined', () => {
      expect(ISpawner).toBeDefined()
    })
  })

  describe('IProcessManager', () => {
    test('should be defined', () => {
      expect(IProcessManager).toBeDefined()
    })
  })
})
