/**
 * Adapters for bridging loopwork infrastructure to subagent packages
 * 
 * Re-exports adapters from @loopwork-ai/adapters for backward compatibility.
 */

export { CliAdapter as CliRunnerAdapter, GitAdapter as GitRunnerAdapter } from '@loopwork-ai/adapters'
export { CliAdapter, GitAdapter } from '@loopwork-ai/adapters'
