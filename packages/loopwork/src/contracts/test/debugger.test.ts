import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { DebugEvent, PrePromptEvent, Breakpoint, DebuggerListener, TUIResult, IDebugger, IDebuggerTUI, DebugEventType, DebuggerState, TUICommand } from '../contracts/debugger'

/**
 * debugger Tests
 * 
 * Auto-generated test suite for debugger
 */

describe('debugger', () => {

  describe('DebugEvent', () => {
    test('should be defined', () => {
      expect(DebugEvent).toBeDefined()
    })
  })

  describe('PrePromptEvent', () => {
    test('should be defined', () => {
      expect(PrePromptEvent).toBeDefined()
    })
  })

  describe('Breakpoint', () => {
    test('should be defined', () => {
      expect(Breakpoint).toBeDefined()
    })
  })

  describe('DebuggerListener', () => {
    test('should be defined', () => {
      expect(DebuggerListener).toBeDefined()
    })
  })

  describe('TUIResult', () => {
    test('should be defined', () => {
      expect(TUIResult).toBeDefined()
    })
  })

  describe('IDebugger', () => {
    test('should be defined', () => {
      expect(IDebugger).toBeDefined()
    })
  })

  describe('IDebuggerTUI', () => {
    test('should be defined', () => {
      expect(IDebuggerTUI).toBeDefined()
    })
  })

  describe('DebugEventType', () => {
    test('should be defined', () => {
      expect(DebugEventType).toBeDefined()
    })
  })

  describe('DebuggerState', () => {
    test('should be defined', () => {
      expect(DebuggerState).toBeDefined()
    })
  })

  describe('TUICommand', () => {
    test('should be defined', () => {
      expect(TUICommand).toBeDefined()
    })
  })
})
