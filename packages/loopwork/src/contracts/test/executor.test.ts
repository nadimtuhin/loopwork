import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { ICliExecutor } from '../contracts/executor'

/**
 * executor Tests
 * 
 * Auto-generated test suite for executor
 */

describe('executor', () => {

  describe('ICliExecutor', () => {
    test('should be defined', () => {
      expect(ICliExecutor).toBeDefined()
    })
  })
})
