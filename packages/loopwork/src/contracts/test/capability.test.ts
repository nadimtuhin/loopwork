import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { CliCommand, AiSkill, PluginCapabilities, CapabilityRegistry } from '../contracts/capability'

/**
 * capability Tests
 * 
 * Auto-generated test suite for capability
 */

describe('capability', () => {

  describe('CliCommand', () => {
    test('should be defined', () => {
      expect(CliCommand).toBeDefined()
    })
  })

  describe('AiSkill', () => {
    test('should be defined', () => {
      expect(AiSkill).toBeDefined()
    })
  })

  describe('PluginCapabilities', () => {
    test('should be defined', () => {
      expect(PluginCapabilities).toBeDefined()
    })
  })

  describe('CapabilityRegistry', () => {
    test('should be defined', () => {
      expect(CapabilityRegistry).toBeDefined()
    })
  })
})
