import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { EmbeddingProvider, EmbeddingConfig, EmbeddingProviderFactory } from '../contracts/embedding'

/**
 * embedding Tests
 * 
 * Auto-generated test suite for embedding
 */

describe('embedding', () => {

  describe('EmbeddingProvider', () => {
    test('should be defined', () => {
      expect(EmbeddingProvider).toBeDefined()
    })
  })

  describe('EmbeddingConfig', () => {
    test('should be defined', () => {
      expect(EmbeddingConfig).toBeDefined()
    })
  })

  describe('EmbeddingProviderFactory', () => {
    test('should be defined', () => {
      expect(EmbeddingProviderFactory).toBeDefined()
    })
  })
})
