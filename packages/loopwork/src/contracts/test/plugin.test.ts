import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { PluginContext, LoopworkPlugin, LoopworkContext, ConfigWrapper } from '../contracts/plugin'

/**
 * plugin Tests
 * 
 * Auto-generated test suite for plugin
 */

describe('plugin', () => {

  describe('PluginContext', () => {
    test('should be defined', () => {
      expect(PluginContext).toBeDefined()
    })
  })

  describe('PluginTask', () => {
    test('should be defined', () => {
      expect(PluginTask).toBeDefined()
    })
  })

  describe('TaskContext', () => {
    test('should be defined', () => {
      expect(TaskContext).toBeDefined()
    })
  })

  describe('LoopworkPlugin', () => {
    test('should be defined', () => {
      expect(LoopworkPlugin).toBeDefined()
    })
  })

  describe('LoopworkContext', () => {
    test('should be defined', () => {
      expect(LoopworkContext).toBeDefined()
    })
  })

  describe('ConfigWrapper', () => {
    test('should be defined', () => {
      expect(ConfigWrapper).toBeDefined()
    })
  })
})
