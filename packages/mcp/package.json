{
  "name": "@loopwork-ai/mcp",
  "version": "0.1.0",
  "description": "MCP (Model Context Protocol) integration plugin for Loopwork",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/*.ts --outdir dist --target node --external bun",
    "test": "bun test"
  },
  "dependencies": {
    "@loopwork-ai/agents": "workspace:*",
    "@loopwork-ai/checkpoint": "workspace:*",
    "@loopwork-ai/loopwork": "workspace:*",
    "@modelcontextprotocol/sdk": "^0.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
