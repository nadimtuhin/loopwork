import { describe, expect, test, beforeEach, afterEach } from 'bun:test'
import { TelegramUpdate } from '../types'

/**
 * types Tests
 * 
 * Auto-generated test suite for types
 */

describe('types', () => {

  describe('TelegramUpdate', () => {
    test('should be defined', () => {
      expect(TelegramUpdate).toBeDefined()
    })
  })

  describe('TelegramTaskBotConfig', () => {
    test('should be defined', () => {
      expect(TelegramTaskBotConfig).toBeDefined()
    })
  })

  describe('TelegramTaskBot', () => {
    test('should be defined', () => {
      expect(TelegramTaskBot).toBeDefined()
    })
  })
})
